# CLAUDE.md — ระบบติดตามการส่งเคลม

กติกาสำหรับคนและ AI ที่เข้ามาแก้โค้ดในโปรเจกต์นี้
อ่านก่อนเริ่มทุกครั้ง ไฟล์นี้ทับ default ของเครื่องมือ

## 1. โปรเจกต์นี้คืออะไร

เว็บติดตามสถานะการส่งเคลมของโรงพยาบาลในเครือข่าย BMS
ผู้ใช้คือผู้บริหาร ผู้จัดการ PM ทีมฝึกอบรม และผู้ดูแลระบบ

**ตอนนี้อยู่ Phase 1: Frontend กับข้อมูลจำลองเท่านั้น**
ยังไม่มี backend จริง ยังไม่ต่อฐานข้อมูล

## 2. กฎเหล็ก ห้ามฝ่าฝืน

### 2.1 ห้ามเก็บข้อมูลส่วนตัวผู้ป่วย

ระบบนี้ดูแค่ว่า "ส่งสำเร็จไหม ติดปัญหาอะไร" ไม่ใช่ระบบเวชระเบียน

- เก็บได้: `vn`, `an` (เลขที่โรงพยาบาลใช้อ้างอิงภายใน)
- เก็บได้แบบ hash: `hn` (เก็บเป็น `hnHash` เท่านั้น)
- **ห้ามเก็บเด็ดขาด**: ชื่อ นามสกุล เลขบัตรประชาชน ที่อยู่ เบอร์โทร วันเกิด
  รหัสวินิจฉัยที่ระบุตัวบุคคลได้ ผลตรวจ
- ถ้ามีความจำเป็นต้องเก็บอะไรเพิ่ม **ต้องถามเจ้าของงานก่อน** ห้ามตัดสินใจเอง

### 2.2 ห้ามใช้ em dash

ห้ามใช้ `—` ทุกที่: โค้ด comment UI copy commit message และคำตอบในแชท
ใช้แทนด้วยการแยกประโยค วงเล็บ `·` หรือ `:`
(`–` en dash ใช้ได้เฉพาะช่วงตัวเลข เช่น `3–50 ตัวอักษร`)

### 2.3 ชื่อตาราง ฟิลด์ สถานะ ห้ามพิมพ์จากความจำ

คำศัพท์ทุกคำที่เกี่ยวกับการส่งเคลมต้องยืนยันจากต้นทางก่อน

ต้นทางที่เชื่อถือได้ เรียงตามลำดับความน่าเชื่อถือ:

1. **API Specification 13Plus (NHSO Digital Platform) v1.3.5.1**
   เอกสารทางการของ สปสช. เป็นต้นทางสูงสุดสำหรับรหัสสถานะและรหัสข้อผิดพลาด
   คัดลอกมาไว้แล้วที่ `docs/domain/nhso-status-codes.md` (66 รหัส)
   และ `docs/domain/nhso-error-codes.md` (654 รหัส)
2. `d:/Work/HOSxP/EclaimFeeSchedule/DATA-DICTIONARY.md`
3. `d:/Work/HOSxP/EclaimFeeSchedule/source/` (โค้ดส่งเคลมจริง)
4. `d:/Work/New folder/ehppcu/EHPPCU/EclaimFeeSchedule/` (ฝั่ง EHP)

ที่ยืนยันมาแล้วและใช้ในโปรเจกต์นี้ อยู่ใน `src/lib/domain/claim.ts`
**ห้ามเพิ่มค่าใหม่ในไฟล์นั้นโดยไม่ยืนยันกับต้นทางก่อน**

⚠️ **สปสช. แก้เอกสารบ่อยมาก** (เวอร์ชัน 0.1 ถึง 1.3.5 ภายในปีครึ่ง)
ก่อนใช้รหัสตัดสินใจสำคัญ ให้เช็คว่ามีเวอร์ชันใหม่กว่าที่บันทึกไว้หรือยัง

## 3. โครงสร้างโค้ด

### 3.1 แยก UI ออกจาก logic ให้ชัด

```
component  ->  hook  ->  api client  ->  server
(หน้าตา)      (logic)    (ยิง HTTP)
```

- **component ห้ามเรียก `fetch` เอง** ให้เรียก hook ใน `features/*/hooks/`
- hook เรียก `lib/api/client.ts` ซึ่งเป็นทางเข้าออกเดียวของ HTTP ทั้งระบบ
- ของจริงมาแล้วเปลี่ยนแค่ `NEXT_PUBLIC_API_BASE` ไม่ต้องแก้หน้าจอ

### 3.2 เพิ่มเมนูใหม่

แก้ `src/config/navigation.ts` ที่เดียว เพิ่ม object เข้าไปในลิสต์
ไม่ต้องไปแตะ `sidebar.tsx`

### 3.3 เพิ่มหน้าใหม่

1. สร้าง `src/features/<ชื่องาน>/` พร้อม `components/` และ `hooks/`
2. สร้าง `src/app/<เส้นทาง>/page.tsx` ที่เรียก component จาก feature
   หน้า page ต้องบาง ไม่มี logic
3. เพิ่มเมนูใน `config/navigation.ts`

### 3.4 สิทธิ์การเข้าถึง

ตอนนี้ทุก role เห็นเหมือนกันหมด ยกเว้น `admin`
**ห้ามเช็ค role ตรง ๆ** เช่น `if (role === "admin")`
ให้ใช้ `can(user, "admin:manage")` จาก `lib/auth/roles.ts`
อนาคตแยกสิทธิ์เพิ่ม แก้ที่ตาราง `ROLE_PERMISSIONS` ที่เดียว

## 4. กฎ UI

### 4.1 ไอคอน

- ใช้ `lucide-react` เท่านั้น ทั้งระบบ
- **ห้ามใช้ emoji เป็นไอคอน** ทุกกรณี
- ทุกปุ่มต้องมีไอคอนคู่กับข้อความที่สื่อความหมาย
- ห้ามสื่อความด้วยไอคอนเดี่ยว ต้องมีข้อความหรือ `aria-label` กำกับเสมอ

### 4.2 สี

สีทั้งหมดเป็น CSS variable ใน `globals.css` **ห้ามใส่ hex ในไฟล์ component**

| ความหมาย | token |
|---|---|
| สำเร็จ | `success` |
| ผิดพลาด ล้มเหลว | `danger` |
| เตือน | `warning` |
| ข้อมูลทั่วไป | `info` |
| สีหลักของแบรนด์ | `primary` (น้ำเงิน BMS) |

**แดงสงวนไว้ให้สถานะผิดพลาดเท่านั้น** ห้ามใช้ตกแต่ง
ไม่งั้นผู้ใช้จะเลิกสังเกตว่าแดงคือของเสีย

สีอย่างเดียวสื่อความไม่พอ ต้องมีไอคอนหรือข้อความคู่เสมอ (คนตาบอดสีต้องอ่านออก)

### 4.3 สถานะการโหลด

- ทุกจอที่ดึงข้อมูลต้องมี skeleton ไม่ใช่จอว่างหรือคำว่า Loading เปล่า ๆ
- skeleton ต้องมีจำนวนแถวเท่าของจริง กัน layout กระโดดตอนข้อมูลมา
- ทุกปุ่มที่ทำงาน async ต้อง disable ระหว่างทำงานและคืนสถานะใน `finally`
  (`components/ui/button.tsx` ทำให้แล้ว ใช้ตัวนั้น)

### 4.4 ข้อความแจ้งเตือน

เขียนให้คนทั่วไปที่ไม่ใช่ dev เข้าใจ ต้องบอก 2 อย่างเสมอ

1. เกิดอะไรขึ้น
2. ต้องทำอะไรต่อ

```
ห้าม:  "เกิดข้อผิดพลาด"
ห้าม:  "Error 500: Internal Server Error"
ใช้:   "ระบบขัดข้องชั่วคราว กรุณารอสักครู่แล้วกดลองใหม่อีกครั้ง"
```

ข้อความของรหัสปัญหาแต่ละตัวอยู่ใน `ISSUE_CATALOG` (`lib/domain/claim.ts`)
มีทั้งคำอธิบายและวิธีแก้ ทีมฝึกอบรมเอาไปบอกโรงพยาบาลได้เลย

### 4.5 วันที่และปี พ.ศ.

ผู้ใช้เป็นหน่วยงานราชการไทย **หน้าจอต้องแสดงปี พ.ศ. เสมอ** เช่น `12 ก.ย. 2569`

- ตัวแปลง ค.ศ. เป็น พ.ศ. อยู่ที่ `lib/date-range.ts` ที่เดียว
  **ห้ามบวกลบ 543 กระจายตามไฟล์อื่น**
- API รับส่งเป็น ค.ศ. รูปแบบ `YYYY-MM-DD` แปลงเป็น พ.ศ. เฉพาะตอนแสดงผล
- 🔴 **ห้ามใช้ `toISOString()` แปลงวันที่เป็น string**
  เพราะมันแปลงเป็น UTC ทำให้วันคลาดไป 1 วันสำหรับเวลาไทย (UTC+7)
  ใช้ `toApiDate()` จาก `lib/date-range.ts` แทน

### 4.6 ฟอนต์

ใช้ **IBM Plex Sans Thai** ทั้งระบบ
**ห้ามใช้ Inter, Geist, Space Grotesk** หรือฟอนต์ Latin ที่ไม่มี glyph ไทย
เพราะภาษาไทยจะหลุดไป fallback ของ browser แล้วสระวรรณยุกต์ลอยเพี้ยน

ทดสอบด้วยข้อความไทยที่มีวรรณยุกต์ซ้อนจริง เช่น
"กำลังตรวจสอบข้อมูลผู้ป่วย" ไม่ใช่ทดสอบด้วย Lorem ipsum

### 4.7 Responsive

รองรับตั้งแต่มือถือถึงทีวี

| ขนาด | พฤติกรรม |
|---|---|
| < 1024px | เมนูซ่อน เปิดด้วยปุ่มแฮมเบอร์เกอร์ |
| >= 1024px | เมนูแสดงถาวร ย่อเหลือไอคอนได้ |
| จอใหญ่มาก | จำกัดความกว้างที่ 1920px ไม่ปล่อยเนื้อหากองกลางจอ |

ตารางกว้างให้ scroll ในกล่องตัวเอง ห้ามดันทั้งหน้า scroll แนวนอน

## 5. Comment ในโค้ด

**default คือไม่มี comment**

- ก่อนเขียน comment ถามก่อนว่าตั้งชื่อตัวแปรหรือแยกฟังก์ชันแทนได้ไหม ถ้าได้ให้ทำแบบนั้น
- เขียนได้เฉพาะตอนโค้ดบอกไม่ได้จริง ๆ เช่น กับดัก เหตุผลที่มองจากโค้ดไม่เห็น
- ถ้าจำเป็นต้องมี ให้ยาวไม่เกิน 1 บรรทัด และอธิบาย **"ทำไม"** ไม่ใช่ "ทำอะไร"
- ห้ามใส่เลข ticket หรือชื่อคนสั่งในโค้ด เก็บไว้ที่ commit
- comment ที่ใส่ตอน debug ต้องลบก่อนส่งงานเสมอ

## 6. ก่อนบอกว่าเสร็จ

```bash
cd frontend
npx tsc --noEmit     # ต้องไม่มี error
npm run lint         # ต้องไม่มี error
npm run build        # ต้องผ่าน
```

**"build ผ่าน" ยังไม่ใช่ "ใช้งานได้"** ต้องเปิดดูหน้าจอจริงในเบราว์เซอร์
ตามเส้นทางที่ผู้ใช้กดถึง แล้วเช็คว่า

- ภาษาไทยแสดงถูก สระวรรณยุกต์ไม่ลอย
- skeleton ขึ้นตอนโหลด แล้วไม่กระโดดตอนข้อมูลมา
- ย่อหน้าจอเหลือขนาดมือถือแล้วยังใช้ได้ ไม่ scroll แนวนอน
- กดปุ่มรัว ๆ แล้วไม่ยิงซ้ำ

## 7. Commit

- ห้ามใส่ `Co-Authored-By: Claude` หรือ `Generated with Claude Code` ท้าย commit
- เขียนเป็นภาษาไทยได้ ตามสไตล์เดิมของ repo

## 8. เอกสารอยู่ที่ไหน

เอกสารทั้งหมดอยู่ใน `docs/` แยกตามหมวด **อย่าเขียนทุกอย่างลง README**

| หมวด | เก็บอะไร |
|---|---|
| `docs/domain/` | ความรู้เรื่องงานส่งเคลม รหัสสถานะ รหัสข้อผิดพลาด |
| `docs/design/` | สี ฟอนต์ หน้าจอ และบันทึกการตัดสินใจ |
| `docs/api/` | รูปแบบการเชื่อมต่อ |

เพิ่มไฟล์ใหม่แล้วต้องเติมในสารบัญที่ `docs/README.md` ด้วย

**ตัดสินใจอะไรสำคัญ ให้บันทึกใน `docs/design/decisions.md`**
พร้อมเหตุผลและทางเลือกที่ไม่เอา คนที่มาทำต่อจะได้ไม่รื้อของที่คิดมาแล้ว

## 9. ยังไม่ได้ตัดสินใจ

เรื่องพวกนี้ยังค้างอยู่ อย่าเดาเอง ให้ถามเจ้าของงาน

- รูปแบบ API ของ BMS Life ที่จะใช้ login จริง
- จะย้ายจากรหัสปัญหาที่ตั้งเอง 8 ตัว ไปใช้รหัสจริงของ สปสช. 654 ตัวเมื่อไร
- จะเก็บ `hn` เป็น hash หรือไม่เก็บเลย
- เกณฑ์แจ้งเตือน เช่น success rate ต่ำกว่าเท่าไรถึงเตือน
- เก็บข้อมูลย้อนหลังกี่ปี
