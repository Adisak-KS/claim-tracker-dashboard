import { LoginView } from "@/features/auth/components/login-view";

export const metadata = { title: "เข้าสู่ระบบ | ระบบติดตามการส่งเคลม" };

export default function LoginPage() {
  return <LoginView />;
}
